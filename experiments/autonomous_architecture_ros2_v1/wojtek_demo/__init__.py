"""The experiment's interactive demo: room sim + walking policy + agent + voice.

`room_app.py` is a fork of `wojtek_rl.room_app` (training keeps its own copy
unchanged).  Named `wojtek_demo` rather than `demo` so it cannot collide with
the training project's `demo` package on PYTHONPATH.
"""
