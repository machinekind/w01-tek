<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>wojtek_sim_bridge</name>
  <version>0.1.0</version>
  <description>
    The world side of the W3 split: wraps the demo's MuJoCo room sim (with
    its walking policy and SCAN planner -- the body emulator) as a
    self-stepping headless ROS node, and bridges the browser/scenario
    websocket protocol onto the agent topics. Everything in this package
    stays off the robot; it is replaced by hardware drivers on deployment.
  </description>
  <maintainer email="maciejgruszczynski@surferseo.com">Maciej Gruszczynski</maintainer>
  <license>Apache License 2.0</license>

  <depend>rclpy</depend>
  <depend>std_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>wojtek_agent_msgs</depend>
  <depend>python3-numpy</depend>

  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
